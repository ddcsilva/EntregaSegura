namespace EntregaSegura.Entities.Models.Enums;

/// <summary>
/// Enum que representa o status de uma conta.
/// </summary>
public enum StatusConta
{
    EmAnalise = 1,
    Aprovada = 2,
    Recusada = 3,
    Bloqueada = 4
}